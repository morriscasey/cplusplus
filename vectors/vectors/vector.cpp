#include <iostream>
#include <vector>
#include <string>
using namespace std;
int main() 
{
	vector<string> names;

	
	for (int i=0; i < names.size(); i++)
	{
		cout << names[i];
	
	}

	cin.ignore();
	cin.get();
	return(0);
}